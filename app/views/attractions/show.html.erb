<h1>Page for the <%= @attraction.name %></h1>
<% if current_user.admin %>
    <h3><%= link_to "Edit Attraction", edit_attraction_path(@attraction) %></h3>
<% else %>
    <h3><%= button_to "Go on this ride", take_ride_path(@attraction) %></h3>
<% end %>